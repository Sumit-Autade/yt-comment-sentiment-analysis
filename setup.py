from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='a chorme plugin to detect the youtube's commenet sentiments',
    author='sumit autade',
    license='',
)
